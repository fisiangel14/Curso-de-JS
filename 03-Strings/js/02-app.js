const producto = "Monitor de 20 pulgadas de 4k";

console.log(producto);

//cobtener el tamaño del string
console.log(producto.length);

//para encontrar el elemento, devuelve la posicion donde lo encuentra
console.log(producto.indexOf('de'));
//para encontrar el elemento, devuelve true o false si lo encuentra o no
console.log(producto.includes('Monitor'))